import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  wrapper: {
    alignItems: 'center',
  },
  title: {
    fontSize: 30,
    marginTop: 50,
    marginBottom: 50,
  },
  logo: {
    height: 200,
    width: 200,
    marginBottom: 50,
  }
});
