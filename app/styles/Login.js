import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  wrapper: {
    alignItems: 'center',
    marginTop: 50
  },
  title: {
    fontSize: 30,
    marginBottom: 150,
  },
  input: {
    width: 200,
    height: 40,
    padding: 10,
    marginBottom: 20
  }
});
